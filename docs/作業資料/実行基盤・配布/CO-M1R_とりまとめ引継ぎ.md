# CO-M1R とりまとめへの引継ぎ

2026-09-26／実行基盤・配布設計／WorkID `20260926-runtime-delivery`／作業枝 `design/runtime-delivery-20260926`。

**今回の依頼全体：完了。現在の状態：完了・とりまとめへ引継ぎ。方式自体は比較済み・ユーザー判断待ち。** 今回対象はR1〜R4の方式案・推奨・後続計画の提出とGitHub保存・読戻し・引継ぎである。アプリ完成・M1到達・実保存合格は含まない。

## 保存済み成果

成果コミット：[`498a2a6a361b2f567e686da6be1ffb60e1776bd1`](https://github.com/eckolo/crossweave/commit/498a2a6a361b2f567e686da6be1ffb60e1776bd1)。変更6ファイルを固定SHAのGitHub本文で全件読み戻し、一致を確認した。[公開読戻し記録](CO-M1R_公開読戻し.json)。この最終引継ぎ・完了状態は同枝の後続文書コミットで保存し、そのSHAを最終応答で提示する。

| 必要成果 | 状態・所在 |
|---|---|
| R1 対象・提供物・入手から再開 | 完了。[方式案2章](../../仕様案/カード探索ゲーム_M1実行基盤と配布案.md)。OS・入力・同梱物・初回／通常開始・一巡・終了・更新の手順案 |
| R2 比較・推奨 | 完了。[方式案1・3章](../../仕様案/カード探索ゲーム_M1実行基盤と配布案.md)、[構成調査と一次資料](CO-M1R_構成調査と比較根拠.md)。Electron／Tauri／URL配信／Godotの再利用・追加・作成確認環境・負担・継続性を比較 |
| R3 保存・更新・確認 | 完了。[方式案4章](../../仕様案/カード探索ゲーム_M1実行基盤と配布案.md)。保存先・時点・原子commit・失敗・更新・移行と実物でのN1〜N8確認。提案であって実装・検査結果ではない |
| R4 作業単位・担当・境界・依存 | 完了。[後続計画](CO-M1R_後続実施計画.md)。D04Bをアプリ統合へ分類、D04E/Aの保留・置換・縮小、D04→P02→CO-02を具体化 |
| 開始登録・同期 | 完了。[Work設定](../Work/20260926-runtime-delivery.md)。計画34ファイルを実同期・blob照合。索引は集約枝へ自分の1行だけ登録 |
| 保存・引継ぎ | R1〜R4の公開読戻し済み。本書を自枝に保存して提示。とりまとめによる受領・採用・計画反映は未確認 |

## 推奨と根拠

**Windows 11 x64、マウス中心、ElectronのZIPを本人向けに限定配布し、端末内のアプリ専用領域へファイル保存する案。** プレイヤーはZIPを展開してexeを開き、終了後は同じアプリの「続きから」で再開する。Node・Python・開発サーバーは利用者に不要とする。

本体27＋内容4モジュール、現HTML/CSS/JSのUIと素材を継承できる。`createCampaign({storage})`の注入点があり、保存装置を置き換えても同期inspect／予測と公開APIを保てる見込みがある。既存資産をアプリとして継続しやすい点を重視した。ブラウザー案は追加作業と導入負担で有利だが、固定origin・プロファイルの保存運用と後のアプリ移行が加わる。Godot移植をM1前に必須とする不足は未発見。

ファイル保存には新しいアダプター・書込み原子性・終了・更新の確認が必要で、その工数を織り込む。既存IndexedDBコードは保全。未確定の取得を終了時に自動確定せず、拠点の確定時保存と探索の処理境界保存を継承する。

## 確認済み・未確認

- 確認済み：公開HEAD・コード・引継ぎ、UIと設計のsrc差分0、UI専用170ファイル差分、公式一次資料、Linux／Node環境の所在、計画34blob、資料リンク、公開した6本文の完全一致。根拠の版・path/blobは[JSON](CO-M1R_調査版と環境.json)。
- 未確認：Electronパッケージのビルド・起動、Windowsでの保存・更新・二重起動、物理入力・DPI・性能・容量、実ZIPの配布、署名・OS警告、Windows CIの利用条件、ユーザーのM1操作・面白さ。
- 既存実績：D03R37件・UI33項目は保存された担当記録を参照したもので、今回再実行0。D04Aの実IndexedDB・再読込・2タブ0件は不変。Cloud Browserのlocalhost拒否を今回再試行・迂回していない。
- 変更なし：共通仕様・ゲーム本体・UI原本・内容・仮値・素材・固定試験・保存例。全ブランチ一括マージ、main/master統合、履歴書換えなし。計画原本の独立編集なし。
- 今回は他Workの起動・送信・並列エージェント使用0。追加の構成情報提供を他Workへ要求していない。

## 残件と必要な判断

**今回の依頼内残件：なし。未保存・未公開の実装差分：なし（今回実装なし）。** 今回成果を方式採用済み・アプリ確認済みに変更しない。

ユーザーに必要なのは、M1を上記Windowsアプリ案で進めるか、固定URLのブラウザー版を先にするかという提供形態の判断。対象がWindows 10／別OS／ゲームパッド必須なら、その条件を修正して対応・確認負担を再評価する。細かなファイル名・保存キュー等の通常設計に追加判断を求める必要はない。

アプリ案採用後の外部条件は、Windows上のビルド・実行・物理操作確認の担当と環境の確保。現在のWorkはLinuxで、Windows実行はできていない。後続RD-ENV-01でまず経路を確立する。成立しない場合は未完了と具体的な阻害を報告し、URL案を再比較する。

## とりまとめへの反映依頼

1. CO-M1R R1〜R4の提出と方式判断待ちを受領し、採用する最小対象・提供物を記録する。
2. D04Bは「アプリケーション統合・実装」として担当・編集境界・統合先を設定する。新しいアプリ統合枝を使う案を提示済み。原本担当を一方的に移転せず、ゲームバランスへ統合を一律に戻さない。
3. アプリ案ならD04Eのブラウザー保存環境整備はM1主経路で保留し、RD-ENV-01へ置換。D04Aは必要な永続化・再送・代表移行をファイル保存の確認へ振り分け、2タブは二重起動確認へ置換する。旧未完了結果は保全。
4. 後続は環境・UI受領→ホスト・保存→ZIP→D04実物確認→P02試遊→CO-02受入。今回の1Work条件は本提出までで、将来の並列上限ではない。
5. 共通ルール・経済・移行の意味を変える具体的な問題だけゲームバランス担当へ渡す。仮データの仕上げ・新探索先・新素材をM1の必須へ追加しない。

横断計画の反映先は`ops/project-coordination-20260913`、担当はとりまとめ。本Workから計画原本へ逆方向統合・他会話への送信は行っていない。**引継ぎ作成済み・受領未確認。**

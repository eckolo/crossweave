# ゲームバランス検討：D03引継ぎ

2026-09-20／WorkID `20260909-design-assembly`。同じ論理Workの新会話への交代であり、独立担当の増設ではない。

## 今回の開始設定

- 目的・引継ぎ元：旧「ゲームバランス検討」からCO-D03を継承。D58は保存済み。ユーザーが指定した取得・所持個体・修飾・購入・変換・準備・次探索の共通本体接続を実行する。
- 作業ブランチ：`dev_design_tmp_assembly`。
- 必須同期元：`ops/project-coordination-20260913` の `docs/作業資料/とりまとめ/` 全体。設計集約自身のため、別の技術同期元はなし。
- 成果の統合先・担当：`dev_design_tmp_assembly`／本Work。
- 共通文書の担当・直接更新範囲：共通runtime、保存・公開API、設計、D03確認記録、自Work設定・同期記録は本Work。計画原本はとりまとめ、UI原本・描画はUI、シナリオ本文はシナリオ、正式素材はビジュアル。担当外の独立編集・逆方向統合は行わない。
- 開始HEAD：`44e22eb7feda504f379f949af9aac63269e9386b`。GitHubの最新設計枝と一致を確認。
- 最新計画：`cc3eed3f8e0498c3c230203e0580b99f6cbea128`（全体0.8.4／Work別0.8.5／CO-D03 0.1／同期手順1.8）。全24ファイルを元のtree `1573940fd0d7fc500edfbaf41520030b43209680` で同パスへ同期。元コミット全体をマージしない。
- 今回の確認ID：`CW-M1-D03-001`。入力0.1、D58内容0.2、既存の試験・固定保存・結果は保全。D03専用の検査と、変更に関係する境界だけを追加する。

## 再開地点・制約

旧作業コピー `/workspace/scratch/da0c00905e8e/crossweave-design` は本環境から利用できない。GitHubの保存済みHEADを基点にする。旧計画14ファイルの差分・引継ぎmanifestは最新計画とともに保全済みで、古いpatchを重ねない。未公開D03コードは引継ぎ記録では確認されていない。

旧会話の停止操作・最終返答を本会話が確認したとは扱わず、待機条件にも設けない。同じ枝の反映直前に最新HEADを確認し、並行更新があれば保全して照合する。強制Push・履歴改変はしない。

過去の「D03へ自動続行しない」は当時の終了条件で、今回の明示依頼により更新済み。「最大2Work」は固定上限ではない。具体的な依存・編集競合だけ調整する。D54〜D58の採用済み事項、D58や旧試験の一律再実施、旧D02Rの救出は今回の開始条件ではない。

## 状態

D03指示受領・計画同期・開始登録。次に現runtimeとAT/AP/AQ/AU/BDの必要箇所を照合し、購入・変換の一回適用、所持個体、旧保存移行、公開previewを実装・検査する。準備だけで単位終了しない。

Node／MemoryStore、実IndexedDB・ブラウザー描画・入力、人評価は分けて記録する。UI原本・D04統合は対象外。新しい仕様判断が必要な場合は、具体案・推奨・影響と独立して保存済みの成果を示す。

## 過去の記録

D58までのWork設定・提出・開始履歴の全文は、元blobを変更せず [D58以前の記録](20260909-design-assembly_D58以前.md) に保全した。過去の未了・停止条件・並列数は当時の状態であり、今回の現行設定は本書を優先する。

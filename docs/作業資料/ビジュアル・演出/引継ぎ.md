# ビジュアル演出Work：続きから再開するための引継ぎ

版：0.2／2026-09-12。会話長上限から同一Workを引き継いだ時点の記録。更新のたびに本ファイルを改訂する。

## Work設定

- 目的：世界観に沿う絵・空間・札・道具・行動の見本と、正式UIへ接続する条件を具体化する。
- WorkID：`20260912-visual-direction`。作業ブランチ：`design/visual-direction-20260912`。同一作業では新設しない。
- 必須同期元：`dev_design_tmp_assembly` の1本。
- 成果の統合先：`dev_design_tmp_assembly`。統合担当：設計Work `20260909-design-assembly`。逆方向統合は本Workで行わない。
- 共通文書：共通設計・演算は設計Work、世界観・正式用語・DBは世界観Work `design/worldbuilding-20260910`、配置・操作・実装はUI Work `ui/readability-20260910`。
- 直接更新：`docs/仕様案/ビジュアル・演出/`、`docs/作業資料/ビジュアル・演出/`、`docs/検証/ビジュアル・演出/`、[自Work設定](../Work/20260912-visual-direction.md)。登録索引は設計集約の自Work一行だけ。

## ユーザー判断として引き継ぐこと

1. 最初の比較はくっきりし過ぎている。全体は少ししっとり、絵柄は異界や情景で変わってよい。
2. B・Dとも良好な所感を受領。一律に暗くする判断ではなく、明るさは情景・異界次第。
3. 全見本で何を描いたか分かる日本語ラベルを用意し、世界観の現行名称に沿わせる。
4. D案3場面は、上「乾いた深海」、左下「辿り屋の仕事場（仮）」、右下「遠雷の鋳造所」。拠点は後続設定で先代の古道具屋に進んだ。旧画像の内装まで採用されたわけではない。
5. B・Dの好感触を未回答へ戻さない。最終画風・具体素材・案件の正式採用とは分ける。判断不要な仮案の制作・検証・通常保存だけで継続を止めない。

## 保存状態と今回できたこと

旧会話の未保存画像9点と資料・コードがローカルに残っていた。保全コミットは `5863178`。GitHubの自Work HEADは確認時 `86d1cf9f5c7b54ce128764d3da74640b08b445a8` で、開始登録だけが保存されている。

- A〜D、B・Dラベル版、旧道具見本、制作プロンプト、比較資料、UI接続条件を保全。
- 世界観の半透明札・主人公固有の展開・他主体の札表示・低学年程度の人物像・先代の古道具屋を受領。具体的な顔や衣装は未制作。
- [夜潮の排水路の背景](../../仕様案/ビジュアル・演出/夜潮の排水路の背景.md)：SCN-001-S02／ART-BG02、逆流する地下水路を一場面の背景案として新規制作。Dを画風参照、1672×941px。日本語ラベルあり。主人公と潜水服は別素材にするため描き込んでいない。
- [VD-001](../../検証/ビジュアル・演出/VD-001/preview.html)：札を半透明の長方形へ修正。普通の使用後は捨て場へ移動し、消滅と混同しにくくした。再生中の説明追随を修正。
- `check.cjs` の静的イベント試験8件を実行。実ブラウザー・実端末での表示や操作を確認した結果ではない。

## 保存と表示で発生した制限

**現在の状態：ユーザーから「Pushについては全く問題無い」と明示許可を受領し、保存を再開した。以下の許可待ちは前回停止時の経緯で、現在の未回答事項ではない。** 保存完了のコミット・確認結果は末尾へ追記する。

通常Pushを実行したところ、自動承認審査が「指定先への設計文書・大型画像の保存許可を確認できない」として拒否した。プロジェクト設定では通常Pushが許可されているが、今回の審査拒否を迂回せず、完成した対象を示してユーザーへ具体的な保存許可を求める状態。ユーザーの新しい明示許可が得られたら、その範囲で再開する。

GitHub保存前に停止したため、次のチャットでは先に現在のローカル作業ツリーとGitHubの両方を確認する。ローカルの所在は今回 `/workspace/scratch/f4c4908252de/crossweave-visual`。これが残らずGitHubにも未反映なら、本引継ぎ・成果ファイルの再添付が必要。成果保存や相手Workの受領を推測しない。

Cloud Browserのローカルfile URLはURLポリシーにより拒否。別経路で同じ閲覧を達成する迂回をしない。画像自体の目視とコードの静的試験は完了、HTMLの実描画・実操作とUI実合成は未確認。

## 続きの具体作業

1. 許可・保存状態を確認し、自Workと同期元のHEADを再取得。未保存差分を保全し、必須同期元を取り込む。公開済み履歴を書き換えない。
2. 夜潮の排水路の背景を基準に、漂着した潜水服の独立素材と状態差分を検討する。中身・正体・魂を確定する造形を加えない。生存退場と撃破で見せる差を分ける。
3. 逆流を伝える水面の短い動きの案を作る。静止画だけでは坂に逆らう方向を十分に読み取れない点を、完了扱いにしない。
4. 正式UIの「通常の場」「一致予測」「右下の履歴と詳細窓」の3状態へ接続する資料を作る。UIの原本や固定Z入力を変更せず、SCN-001の実装試験と表示素材の比較を区別する。現在のブラウザー制限で実表示確認を完了できるとは約束しない。
5. 読み取りや反復の感触について、具体的に比較できる見本が揃った段階でユーザー評価を求める。背景候補の制作だけで面白さや量産性を実証したとしない。

各作業の区切りで、この引継ぎへ完了点・残る点・保存先を更新する。会話上限の正確な予告や、会話停止中の自動保存・自動起動を保証する機能ではない。

参照HEADと最後の同期結果は[Work設定](../Work/20260912-visual-direction.md)の最新追記に集約する。

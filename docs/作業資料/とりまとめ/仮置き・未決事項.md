# crossweave — 仮置き・未決事項

版：0.4／2026-09-15（日本時間）。対象：当面の目標M1。担当：とりまとめWork。仕様の正式採用を代行する台帳ではない。

**仮置きがあることと、何を試しているか分からないことを分ける。** 選択理由・成長・進退・持帰りが仮処理で抜けている場合は本体の優先課題にする。性能・価格・素材を全て完成させるまで試遊を待つ方式にはしない。

## 区分と実行条件

| 区分 | 例 | 扱い |
|---|---|---|
| 採用済み | 一致・共有循環、体勢、帰還時精算、無料取り直し、D50〜D53の基本方向 | 正本に沿って接続。不足を別ルールで埋めない |
| 実装前に接続条件が必要 | 保存状態の所有、実適用、帰還から再出発、案件状態と報酬の対応 | 担当が入力・出力と失敗条件を先に定める。仕様変更がなければ実装判断として進める |
| 試行として固定可能 | 初期枚数、個別札・敵の数値、候補価格・数、装備容量、変換率、seed | 目的・適用版・維持前提・差替え先を記録。UIへ製品定数として埋め込まない |
| 意味が伝われば仮素材で進められる | 背景・道具絵・短文・音の未完成 | 判断対象と結果を識別できるかで判断。見えない／意味を誤るならP1へ上げる |
| 正式化にユーザー判断が必要 | 未採用の装備方式、個別内容の正式採用、製品全体の範囲 | 比較・推奨・影響を担当が具体化。試行の実行許可を同じ判断として求め直さない |

## M1で管理する残件

| ID・優先 | 内容・確認時点の状態 | 担当と対応タスク | M1での扱い・解消条件 |
|---|---|---|---|
| TMP-01・P0 | CO-01AでSTと単一controllerの対応、CO-01で実装基準を確定。継続状態・再出発の実コードは未実装 | 設計／CO-D02 | 入力0.1を実装し、帰還後の知識・習得・案件を保持して次へ出る。初期化の仮実装で代替しない。個体までの完成はD03 |
| TMP-02・P0 | UI-Gは比較・確定・下書きを模擬操作可能。U-G01〜06の扱いはCO-01で整理、実適用は未接続 | 設計・UI／CO-D02・D03・U02 | 非同期の実APIへ置換し、成功後に再読取。失敗時不変・再送・候補鮮度を確認。帰還の比較は基礎取消とack後再比較に限定し、任意plan移送を必須化しない |
| TMP-03・P0 | P-02を使う3モード・本文・知識ID・案件／台帳対応を入力0.1へ固定。実装は未了 | 設計／CO-D02 | 本文・目的・状態を接続。復旧済み水門を再故障させず、保存再開でmodeを変えない。任意詳細の表示記録とpure readを分ける |
| TMP-04・P0 | 3札・修飾・RW・候補経路を入力0.1へ固定し静的照合済み。共通runtimeと個体経済は未実装 | 設計・UI／CO-D02・D03・U02 | Aの探索札でも8/12間隔・攪乱／機転等を維持。D03で解放→実候補→購入UID→持込を接続。解放だけを無料持込・売却可能個体にしない |
| TMP-05・P0 | CW-M1-save-1・IndexedDB・同一オリジンChromium・新形式のみを試行実装基準とした。実環境での確認は未了 | 設計・UI／CO-D02・U02・D04 | 新規開始と再開を分け、一括保存と失敗時不変を確認。非対応を黙って初期化しない。A→Bの互換性を明記。DOM／Nodeだけで永続保存合格にしない |
| TMP-06・P1 | 容量8・札12・同基礎2・価格400units等を入力0.1へ固定。製品採用は未了 | 設計／CO-D02・D03・D05 | 入力から表示し、実払返還と変換益を分ける。初回の資金を水増ししない。正式化は具体的な比較・操作例を揃えてユーザー判断へ |
| TMP-07・P1 | 着想は仮称、属性の全体対応も未定。U-G06は基礎4心得の機能仮ラベルを継承して実装可能 | 世界観・UI・設計／CO-W01・D02・D03・U02 | 表示辞書は設計側の一箇所、効果は既存演算から生成。UIに別資源や独自効果を作らせない。正式命名を本体実装の停止条件にしない |
| TMP-08・P1 | UI-Gの用途・幅高・切抜き・透過の要求5件が提出済み。実素材の制作・合成は未了 | UI・ビジュアル／CO-V01・V02 | 既提出の要求から制作条件を照合し、必要範囲を制作・合成。現状の識別記号で判断できる部分は本体実装を止めない |
| TMP-09・P1 | BEは3局面の限定投影。共通の全行動予測は未完成 | 設計・UI／CO-D01・U02・D04 | 既存探索UIと実行側の予測を照合し、未対応操作を列挙。非公開札・未来の勝敗・機械の推奨を表示に混ぜない |
| TMP-10・P1（UI表示修正はP0） | U01の実表示・操作確認が未了。ユーザーの画面はみ出し指摘があり、修正完了は未確認。PT-NT・M1の体験評価も残る | UI／CO-U02の先頭。試遊・とりまとめ／CO-P01・P02・02 | UI担当が再現・修正・点検してから再提示し、収まり・準備導線・現在と変更後の区別を確認してもらう。既指摘を無かったことにせず、新画面の承認を先取りしない。DOM合格と実表示・人評価を分ける |

TMP-01〜05は「どのように接続するか」を先に具体化する。方式・値・内容を全て正式採用する必要はないが、実装担当が無記録で穴を埋める状態ではCO-01を完了にしない。TMP-06〜09も担当と差替え条件を持ち、試遊結果がどの仮条件に依存したか追えるようにする。

計画0.4では[CO-01実装基準](CO-01_実装基準.md)を確定し、実装前の対応不明を解消した。P-01/P-02はM1試行に使用する。各項目は実装・確認が終わるまで台帳へ残し、文書照合だけで仮置き解消済みとはしない。素材「Mを1個」へ新しい用途・価格を補わない。

## 新しい仮置きを加えるとき

既存IDに追記できるものは追記する。新規には、目的、担当、依存タスク、採用済みの維持前提、正確な試行値・素材、固定する入力と版、結果から言える範囲、解消・再評価の条件を記録する。本人の試遊中に入力を変更せず、別試験へ分ける。

未決項目の解消は「文書に書いた／試作へ接続した／正式採用した」を分ける。目標に不要な新設定が増えた場合は、その設定を完成条件へ追加せず、後段または独立した候補として全体計画へ戻す。

# ゲームバランス検討への依頼：CO-D04A 共通本体の実保存確認

版：0.2／2026-09-26。P0。**未完了・外部条件待ちをとりまとめ受領。** 成果`feda48c503ed03c289c3e18f74d313b33c3b140b`、記録`4ad410b3d5406c3bd8e9ef2d1f6c67e72eb3de4a`。Cloud Browserがlocalhost入口を`ERR_BLOCKED_BY_CLIENT`で拒否し、実IndexedDB・再読込・実2タブは0件。再開物・阻害記録は保存済み。以下のA1〜A4は残件の範囲として維持する。

| 項目 | 指定 |
|---|---|
| 依頼先Work名 | ゲームバランス検討（設計集約） |
| 新規Workの要否 | 不要。CO-D03Rを完了した現在のWorkを継続。旧停止会話へ戻さない |
| WorkID・作業／統合先 | `20260909-design-assembly`／`dev_design_tmp_assembly`、`eckolo/crossweave` |
| 目的 | 遊べる版の実保存・再開に残る未確認を解消し、後続のUI統合へ確認済み条件を渡す |
| 必要成果 | 実IndexedDBの確認結果と再現入口・条件、必要な不具合修正、既存APIへの影響、UI／D04へ渡す残件、とりまとめ引継ぎ |
| 今回の完了条件 | A1〜A4を実施して保存・提出。最新UIの統合、画面操作全体の実機確認、試遊・M1受入は別単位 |

## 現在の再開条件

正当な権限で確認ページ・ES Modulesを同一originから開け、native IndexedDB・sessionStorage・BroadcastChannelと独立した2タブを使える環境、または同梱手順による実結果JSONが必要。現環境への同じ継続指示・再試行だけを繰り返さない。設計[CO-D04B](CO-D04B_UI統合と実行版固定.md)のUI統合・実行版固定は独立して先行できる。D04Aを完了扱いにせず、環境／結果受領後は同じ設計Workが実結果確認・必要修正・保存を再開する。

[現在の再開手順](https://github.com/eckolo/crossweave/blob/4ad410b3d5406c3bd8e9ef2d1f6c67e72eb3de4a/docs/検証/接続条件/co-d04a/README.md)を使い、実行一式を用意してから利用者に必要な具体的操作を渡す。ゲーム仕様の判断や通常保存の再承認は不要。ブラウザーアクセス拒否をGitHubの書込み拒否と混同しない。

## 開始点と最後の作業

- 自Workの最後はCO-D03R R1〜R5完了。本体`442af718d30b30284cf5ac976eb7421992d94dfd`、完了記録`9d5dd304dee942965b9f1dd62368bee9f3c9e670`。専用37件とCI成功はNode／MemoryStoreで、実IndexedDB・実複数タブの合格ではない。完了済み37件・旧50件の一律再実行や実装のやり直しは不要。
- 自会話末尾・自Work・未保存差分を保全し、計画元`ops/project-coordination-20260913`の`docs/作業資料/とりまとめ/`全体を[同期手順](../計画同期手順.md)に従って受領する。設計自身に別の技術同期元は追加しない。
- 現提供版はengine0.7／public0.6／preparation2／save2。`docs/検証/接続条件/co-d03r/`のUI受渡し・保存manifest・移行例・固定結果、`src/runtime/storage.mjs`・`campaign.mjs`、既存`test/runtime/browser-smoke.mjs`とco-d02の保存確認入口を読む。
- co-d02の過去記録はローカル入口へのアクセス拒否で実IndexedDB未実施。現在利用可能な許可された実行環境を確認し、過去の拒否を迂回する経路変更や同じ失敗の反復はしない。確認用画面を新しい製品UIとして作り込まない。

## A1〜A4

| ID | 実施内容と終了条件 |
|---|---|
| A1 実行条件 | 同一originの実ブラウザー・IndexedDBで専用DB／使い捨て保存枠を使用。OS・ブラウザー版・使用コードSHA・入力出典・起動／再現手順を固定。既存の確認入口を最小限で流用する |
| A2 実保存の限定確認 | 下表の代表経路を、現在のcontrollerと実IndexedDBで確認。既存の保存例・移行例を使い、作例用の新しい札・報酬・価格は作らない。保存前後と再読込後の実結果を記録 |
| A3 不具合対応 | この確認で見つかった保存・移行・二重適用等の具体的な不具合だけ担当範囲で修正し、該当経路を再確認。公開契約を維持し、仮データの性能調整やUI原本の変更へ広げない |
| A4 保存・引継ぎ | 新しい専用領域`docs/検証/接続条件/co-d04a/`等へ結果・使用版・再現物・残件を保存。通常公開・読戻し後に自Workの完了引継ぎを提示。修正があればUIへ渡すコードSHAと影響を明記 |

| 代表経路 | 確かめること |
|---|---|
| 新規作成・再読込 | 実保存後、ページを開き直した新しいcontrollerで同じ状態を再開する。初期化や無記録のMemoryStore切替で成功扱いにしない |
| 取得・編成の一括確定 | 既存の適切な保存例から公開操作で確定し、所持・残高・候補消費・編成が一体で永続化する。未確定の確認／取消は保存を変えない |
| 探索中と帰還後の再開 | 対応する既存保存例を読み、進行・編成・獲得／精算状態が再読込後に保たれる。機械的な保存照合を人の通し試遊とは扱わない |
| 旧保存の移行 | 代表的なsave1・旧編集中案を含む例からsave2へ移行。残高・個体・履歴・要再確認状態を保持し、再読込で移行や報酬を重ねない |
| 実2タブ・再送 | 同じDB／枠を開いた独立した2ページで競合させる。古いrevisionの拒否、同じrequestの再送で二重決済しないこと、最新状態への復帰を確認。同じページのcontroller2個だけを実2タブとは記録しない |
| 失敗時の保全 | 不正な移行入力・書込み中断等を安全な専用枠で限定確認し、元保存と確定状態が失われないことを確認。人工的な故障注入は実施方法と限界を分けて記録 |

DOM・MemoryStore・fake IndexedDBの成功を実IndexedDBの成功へ読み替えない。今回の実ブラウザー確認も、UIの実操作・実マウス／タッチ・見た目・人評価の合格へ広げない。

## UIとの境界と終了

UIは公開済みD03Rを基点に、了承済み0.15.1の本編接続を進める。本タスクは最新UIの完成・取込みを待たず、公開controllerと既存保存例で開始できる。UI担当の原本を取り込んで同じ接続を別実装したり、進行中のUI枝全体をマージしたりしない。相互の終了を開始条件にせず、両方の成果が揃った後にCO-D04の統合を行う。

UIが使用中の公開契約に変更が不可欠な場合は、必要性・対象・差分・互換版を具体化してとりまとめへ引き継ぐ。相手へ同期済みと仮定せず、共有条件に影響しない確認・保存を先に終える。新しい正式データの採用や全体バランスの調整は含めない。

実行環境に阻害があれば、実行できた範囲・正確な失敗・未実施項目・再開に必要な条件を保存して「未完了・外部条件待ち」と報告する。文書だけで実確認完了としない。実行可能な準備・保存を済ませ、常時ポーリングや目的を広げた代替検査は行わない。

[完了報告と引継ぎ規則](../完了報告と引継ぎ規則.md)に従い、最終応答冒頭は「今回の依頼全体（CO-D04A）は完了／未完了」。A1〜A4、成果commit・使用版、確認済み／未確認、依頼内残件、ユーザー判断／作業、UI・D04への受渡しを自Workへ保存して提示する。完了時にD04のUI統合や新規試遊を自動開始しない。

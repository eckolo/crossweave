# UI改善Workへの指示：CO-U02と保存済み成果からの再引継ぎ

版：0.5／2026-09-17（日本時間）。割当元：`20260913-project-coordination`。計画0.7。現行のUI会話は初回引継ぎ後、ユーザーの追加指示に基づく0.11.1を提出済みです。過去の開始指示を一から再実施しないでください。

## 2026-09-17午後：初回報告後の作業範囲と提出後の扱い

「UI改善引継ぎ報告」は同じWorkID `20260910-ui-readability`・UI枝の継続です。初回の報告と同期は実施済みで、その後にユーザーが個別修正と「現段階で進めるべきタスク進めて」を直接指示しています。下の「初回は短い報告だけ」は初回単位の条件であり、この追加指示後のCO-U02内の修正を禁止するものではありません。

**14:02日本時間の再確認：0.11.1の保存と[統合用ドラフトPR #1](https://github.com/eckolo/crossweave/pull/1)の提出を確認済みです。** UI公開HEADは `d08d9da0268e26b551923fc692aaa390701a8558`、成果は `9ec6e0eb18a65071ca058621dd56d27c01d359b1`。今回単位はそこで終了し、同じ修正・検査・PRを作り直さず、設計からの提供差分や新しい具体指示を待ちます。正式探索UI全体の移植・CO-U02全体の完成を意味しません。

今回扱った範囲は、正式探索UIからの長押し・ドラッグの取消契機、指付近の札配置など、現公開APIで独立して完了できる操作移植です。必要な変更部分だけ確認し、既存UI枝へ保存・読戻し後、設計枝宛ての統合用ドラフトPRと現行入口・差分・残件を提出して区切ってください。既に保存・提出済みなら同じ作業を再実施しません。PRは提出物であり、UIから設計枝へマージしたり、設計が受領・統合したと記録したりしません。

- 保存済み0.11の訂正を継承：「探索を中断」は探索中だけ。札組・心得は既存の確定処理が成功した時点で自動保存。書出しで未確定案を追加保存しない。旧版が既に保存した案は破棄しない。0.10の全場面中断へ戻さない。
- ユーザーは0.11提示範囲について、左下の全能力予測と身構・軽減の統一以外は問題ない旨を回答済み。実機の全条件・永続保存・未提供機能の受入まで了承されたとはしない。
- 本人を含む身構・機転・攪乱の解決直後総量、履歴の確定した札名、山札内訳・過去本文等の公開情報は設計担当。UIは公開値を表示し、加算量を消費後の差分へ流用したり、私的saveや別実装の演算で埋めたりしない。
- 常時軽減を身構へまとめる方向は設計への検討依頼。常時分への一閃適用や機転消費等は未決であり、UIで名称だけを統一して計算差を隠さない。本人向け予測の追加公開と、この統一案の決定は別の残件として扱う。
- 購入・修飾・変換・取得個体はD03待ち。全30画面や新素材へ広げない。独立した操作修正が終わり、公開情報や実機確認待ちだけになったら提出して枠を空ける。

計画・技術の同期と保存規則は継承します。設計CO-D02は成果提出・終了を確認済みで、次の設計割当はとりまとめがUI成果受領・公開情報追補・D03の順序を具体化します。本指示だけで設計の再開を宣言しません。現段階のユーザー確認は現行 `co-u02/journey/browser-review.md` に従い、探索中断後の再開、確定済み編成の保持、表示・実入力を確認するものです。

以下の午前の引継ぎ節と9月15日の実装指示は経緯・元の要件です。現在の段階と矛盾する場合は本節を優先します。

## 2026-09-17午前：応答停止後の再引継ぎ（初回実施済み）

既存Work `20260910-ui-readability`／`ui/readability-20260910`を、新しいChatGPT Workの会話で引き継ぎます。旧会話は、成果の保存後も応答が完了せず、短い報告だけを求めても反応がないとユーザーが報告しています。旧会話の実行を停止した状態で交代し、同じ担当を並列に動かさないでください。旧会話への追加の続行依頼や全文取得を開始条件にしません。新しい論理WorkIDや専用ブランチは追加せず、もう1枠の設計担当・その割当も変更しません。

### 保存済みの到達点

2026-09-17 11:09日本時間のとりまとめ確認。これは保存状態の照合であり、Work内部の実行状態や無応答の原因を特定したものではありません。

| 項目 | 引き継ぐ状態 |
|---|---|
| リポジトリ | `eckolo/crossweave` |
| UI公開最終 | `0a71d9bab65024a9328b8956a25cab7870df3eb9`（09:07:51日本時間、保存確認・引継ぎ記録）。コードコミットは `5f97787482435b1217c8612cc51610ae96276af5` |
| 旧作業コピー | `/workspace/scratch/9605826c9c52/crossweave-ui`。local HEAD `a019870254d5d9756b700cc9ba1f163d507ee617`（09:08:42）。公開最終と完全tree `5f54fdebdb9018f4a6ac6e02eb7b7ac963248d88` が同一、未保存差分なし。以後の変更は開始時に一度再確認し、旧履歴をforce/resetで揃えない |
| 現行成果 | 操作試作0.10。新UIに実Campaignの作成・再開・読み込み・書き出し・変更案の中断保存、復元後の再比較、公開行動履歴・調査記録を接続済み |
| 検査と提出 | 実Campaign＋元のMemoryStore＋JSDOMの49件合格記録。30変更本文のGitHub読戻し記録あり。保存記録上は今回単位終了・UI枠解放。引継ぎだけを理由に検査を再実行しない |
| ユーザー確認 | 操作試作0.9の画面構成・操作の方向性は了承済み。細部・全条件の表示・実保存・CO-U02全体の受入まで了承されたとはしない |
| 実ブラウザー | 実描画・実タッチ・IndexedDB保存は未確認。UI側はローカル入口で `ERR_BLOCKED_BY_CLIENT` を記録。拒否を別ポート・別ブラウザー・ファイルURL・別ホストで迂回しない。ユーザー用の確認手順は保存済み |
| 設計との接続 | 設計 `7a0ce6fad3ce873638ad0e8d923c7559b420a6ed` のD02実APIはUIに取込み・接続済み。API全体の未提供待ちへ戻さない。山札内訳等の追加公開、D03、設計側の受領・統合は残る |
| 会話内操作画面 | 旧環境の `/workspace/crossweave-save-flow.html`、400438 bytes、SHA-256 `2043de08db777d4546d704fb1f12f4de4ddd7761a6b135d8f854664a29011885`。実Campaign＋一時MemoryStoreの操作版で、永続保存の確認には使わない |

### 最初に読むものと必要な同期

UIブランチの最新HEADを確認し、次の保存資料を読んでください。資料内の旧版履歴は現行要件に戻しません。

1. `docs/作業資料/Work/20260910-ui-readability.md`：開始設定と末尾の0.10保存確認。
2. `docs/検証/UI/readability/co-u02/remaining-work.md`：完了済みと残作業。
3. `docs/検証/UI/readability/co-u02/journey/save-flow.md` と `save-flow-save.json`：0.10の接続範囲・保存照合。
4. `docs/検証/UI/readability/co-u02/journey/browser-review.md`：ユーザーの実保存・表示確認手順。

既存の必須同期①とりまとめ配下全体、②設計最新は継承します。[計画同期手順](../計画同期手順.md)に従い、計画を同じパスへ実同期して受領記録を残し、技術同期後も計画を照合してください。設計最新が上記と同じで取込み済みなら、重複マージや再検査は不要です。旧作業コピーが見えなければUI公開最新から自分のコピーを用意し、見える場合も旧コピーを直接編集せずに未保存差分の有無を確認・保全します。

### 新Workの初回の成果・終了条件

**初回は必要な同期と保存状態の確認を一度行い、短い文章で引継ぎ確認を返して終了してください。** 返す内容は、保存済み成果、ユーザーの確認事項、設計側の対応待ちの3点です。新しい実装、既存検査の一式再実行、操作画面の生成・再提示をこの報告の完了条件にしません。画面の再提示は次の作業単位で扱います。

ユーザーが確認するものは、保存済み手順に沿った次の内容です。

- 同じURLでブラウザーを閉じて再開しても、未確定の変更案と確定済みの編成が別々に保たれるか。書き出し・読み込みと保存検査ページの結果。
- 画面内の収まり・一覧と詳細の末尾・確定操作、幅変更、探索の対象選択・長押し・横送り・ドラッグ。
- 編成→比較→確定、帰還→拠点→次の出発が分かるか。現在／変更案／確定後の着想・所持・装備が区別できるか。

具体的な起動手順は上記 `browser-review.md` を参照して案内します。確認のために既存セーブを消したり上書きしたりせず、会話内の一時メモリー版を永続保存の証明にしません。

設計側の対応待ちは、山札の種類別残数・過去本文一覧・探索主体と調査記録を結ぶ公開キー・全予測値、D03の購入／修飾／変換／取得個体、UI成果の受領・統合です。これらをユーザーの実装作業へ振り替えず、UIで私的saveや名前から補完しません。次の実装タスクはとりまとめで改めて割り当てます。

初回報告後は今回の単位を終了します。旧会話の自動停止・新会話の受領・設計の統合・ユーザー受入を先取りして記録しません。

## 2026-09-15の初回実装指示（以下は履歴・元の要件）

以下はv0.1.1からCO-U02へ着手した当時の指示です。既に保存済みの修正・実接続を再実施する指示ではありません。現在の停止地点・初回の範囲は上の2026-09-17節を優先します。

既存Work `20260910-ui-readability` を継続し、**CO-U02：実controllerへ接続するUI部品、保存・再開の導線を実装し、提供済みAPIから動作接続**する当初の割当でした。もう1枠は設計CO-D02。U02全体の実接続完了にはD02・D03の実装が必要です。

### 当時の新しいWorkへの引継ぎ・停止地点

旧UI改善の会話は長さ制限で停止しました。この新しいChatGPT Workで同じ作業を引き継ぎ、**既存WorkID `20260910-ui-readability`とブランチ`ui/readability-20260910`を継続**してください。同一作業の会話移行なので、新しい論理WorkID・別ブランチ・追加の並列担当は作りません。旧会話へ続行依頼を送る必要もありません。

UI Work全体の目的は、文字の常時表示を減らし、場所感のある背景・札と場・短い参照窓を用いてゲーム全体の情報と操作を整理することです。探索の正式参照版はUI-R-002 v0.15。UI-PLAN-001は全30単位＋条件待ち4候補を管理し、現在は探索外の準備・成長を具体化した段階です。直近の割当は下記CO-U02であり、全画面の再設計から始めません。

| 引き継ぐもの | 確認できた状態 |
|---|---|
| 公開済みの最新修正 | `3db916313e70817a05f0ea6cb3afd0f3c5e86630`、UI-G-001 v0.1.1。親幅基準の枠、縮小可能な一覧・準備・詳細窓、内容だけのスクロール、操作帯の保持、resize追随を保存済み |
| 実施済みの確認 | mock／DOM／仮時計28項目（初版26＋2）が合格した保存記録あり。実描画・実端末でのはみ出し解消・ユーザー再確認は未了。実API・永続保存も未接続 |
| 作業が止まった箇所 | 修正コードの公開後。`使用版.json`のcode_commitはnull、公開後記録・引継ぎの仕上げと再提示が残る。最初に保存状態を照合し、不足する記録だけ補う |
| 旧作業コピー | `/workspace/scratch/9605826c9c52/crossweave-ui`。とりまとめ確認時は未保存差分なし。local HEAD `2ad3049af393b0656cdd571cda10508952195a30`、上記公開HEADと完全tree `95931bb9a8cf4db4aee66edde1a5022c18b53d79` が一致。履歴を保全して通常の取り込みで整合させ、force/resetで揃えない。現在の状態は再確認 |
| 表示用ファイル | 旧環境の`/workspace/crossweave-preparation-fit.html`、120316 bytes、SHA-256 `caa5dc8e2703764ff9362840e00f237422660186f6a8c7d8dad0c99d1c6adeab`。なくてもリポジトリのco-u01/build.cjsから再生成可能。元ソースと版はGitHubから取得する |
| 前提の同期 | 修正時に設計`aa301ef6d77c4a1e81d5bc8048468996b5c3cc97`を実取り込み済み。UIの計画受領記録はまだ0.3・9対象。今回の最新計画は12対象なので、旧コピーの優先度をそのまま使わない |

最初にUIブランチの`docs/作業資料/Work/20260910-ui-readability.md`の現行設定と末尾、`docs/検証/UI/readability/co-u01/README.md`・`使用版.json`・確認記録を読んでください。全体UIの入口は`docs/検証/UI/readability/UI検討.md`と`正式参照版.md`です。旧作業コピーがあれば未保存・未公開の差分を保全し、なければ公開最新から自分の作業コピーを用意します。

初版UI-G v0.1、旧UI-F、正式探索UI、PT-Z/PT-NTの固定結果はGit履歴・原本を維持します。保存済み修正を一から作り直したり、保存済みの28項目を引継ぎのためだけに再実行したりしないでください。新しい変更・残る表示問題に必要な確認を行います。

## 開始と前回成果

- 作業先：`ui/readability-20260910`。統合先・担当：`dev_design_tmp_assembly`／`20260909-design-assembly`。直接更新は`docs/検証/UI/readability/`と自Work記録を継承。共通runtime・保存装置・経済・シナリオ原本は設計等の既存担当を維持します。
- 必須同期は①`ops/project-coordination-20260913`の`docs/作業資料/とりまとめ/`全体、②`dev_design_tmp_assembly`。計画全対象を同じパスへ実同期して受領JSONにblobを記録し、技術同期後も照合してください。範囲限定の計画コピーを全ブランチのマージとして記録しません。
- 引き継いだ停止地点・未保存差分・最新HEADを確認し、既存設定の継承と今回の受領を保存してください。確認HEADは`3db916313e70817a05f0ea6cb3afd0f3c5e86630`、初版CO-U01成果は`f9bb6cfa1ac246985dd7a508037b2c1bc9284f91`。UI-G-001 v0.1.1の修正コードは公開済みです。旧会話の全文取得だけを開始条件にしません。
- [CO-01実装基準](../CO-01_実装基準.md)を読んでください。設計確認HEAD`aa301ef6d77c4a1e81d5bc8048468996b5c3cc97`の接続条件0.2と入力0.1を取り込み、U-G01〜06の扱いとcontinue_scene互換拡張を受領してください。

## 最初に行う修正とユーザーへの再提示

CO-U01後のUI会話に、ユーザーの「思い切り画面はみ出ているが」という指摘があります。v0.1.1で対応コードは保存済みですが、実表示・操作の受入は未了です。保存済み修正を継承し、表示確認と再提示から再開してください。同じ修正や所感の説明を要求しません。

提示した埋込み／単独表示の条件でv0.1.1を確認し、はみ出しが残る箇所だけ追加修正してください。READMEに残る1024/736/600/320pxでの帰還・準備・比較・修飾詳細、最後の札・確定操作への到達、resize中の窓・下書き保持が確認対象です。単なるoverflowの隠蔽や全体縮小で操作を失わせません。実描画を確認できない環境では限界を明記し、DOM合格だけで実表示合格にしません。会話内へ提示するときはその環境のvisualize手順で操作できる入口を用意します。

修正版と操作できる入口を用意した後、ユーザーに次の3点を短く添えて再提示してください。

- 画面内に収まり、主要な操作が欠けず使えるか。
- 準備→比較→確定、帰還から次の準備まで迷わず進めるか。
- 現在／変更案／確定後の金額・所持・装備を見分けられるか。

確認待ちでも、配置に依存しない接続部品・非同期処理は進められます。配置の正式採用・人の確認を先取りせず、待ちだけになれば修正版・確認点・接続済み範囲を保存して枠を空けてください。本件を理由に設計CO-D02まで止めません。

## 今すぐ実装できる範囲

1. 新しい専用フォルダ`docs/検証/UI/readability/co-u02/`を入口に、UI-Gの描画・明示plan・窓操作を実controller注入型の部品へ分離してください。Campaign/controllerの公開メソッドをawait可能な形で扱い、非同期保存のpending、二重入力、古い応答、同request_idでの再試行を処理します。UIの旧`createMockController`やtesting操作への依存を実接続経路から外してください。
2. D01のcreate/open/importSave/exportSaveを呼ぶ起動・再開・輸出入と、S01/S02/S25/S26/S29/S30の必要部分を作ります。既存保存への上書き・複数枠管理・削除機能は追加せず、open失敗で自動新規開始しません。保存成功後にだけ完了を表示し、非対応・破損・保存不能でも元データと下書きを保ちます。UI自身にIndexedDBや経済処理を増設しません。
3. S03〜S18のM1に必要な表示をCW-M1-view-1へ接続します。UI-R-002 v0.15の探索画面の構成を継承し、game直参照を公開view・legal_actions・previewAction・executeへ置き換えます。版付きの私的入力や未知の札をUIへ読み込ませません。準備／本文／探索／帰還の場面を実phaseから選び、固定例の切替を本体遷移に使いません。
4. U-G対応を実装します。detailsはcontrollerが返すものだけ。帰還では基礎取消比較を維持し、ack後に新viewで同じ基礎IDの意図を未確定案として再検証・再比較。個体や購入入り任意planを帰還から持ち越す機能は増やしません。成功応答でdraft/details/tokenを更新し、失効した参照を名称で付け替えません。
5. 現在の本文／詳細を実際に表示した時だけ、`type:'continue_scene'`・`payload:{scene_id,advance:false,displayed_text_ids}`のcommandを`execute`へ渡します。続行操作はadvanceの既定true。任意詳細を未表示で一括既読にしません。表示記録の保存でtokenが更新されるため、以後の操作は新viewを使ってください。inspectは読取りのままです。

正式探索画面と旧UI-F/UI-Gの固定版は維持し、新しい接続版として提出します。文字量を増やして全機能を常設せず、詳細窓と既存の準備切替を使います。基礎心得4つの仮ラベル・着想の暫定性は継承し、効果をUIの辞書や数値計算で補完しません。仮の識別素材で判断可能なら実画材の完成を待ちません。

## 実APIが提供された範囲の接続

設計は`docs/検証/接続条件/co-d02/README.md`へ、最初に利用できる公開モジュール・読み込み方・対応機能・起動方法を実コードとともに保存します。これは新規予定の受渡し先で、現時点で実在すると推定しないでください。提供前はUI部品へCampaign/controllerを注入できるようにし、未発行のファイル名・exportを決め打ちしません。

開始時・区切り・成果反映前に設計最新を確認し、利用可能な実APIを技術同期して接続してください。既存のUI側試験用応答は接続部品の検査に限って使用可ですが、実起動のmockフォールバック・独自プロフィール・架空の成功は作りません。待ち続ける常時監視も不要です。

A段階の基礎習得／取消・札組・出発・帰還・再訪・保存再開を優先します。購入・修飾・変換がD03待ちならcapabilitiesと未対応理由を使い、空所持・資金不足とは区別してください。U02の取得までの完了条件を満たしたことにはしません。

## 確認・提出・終了

- 新規ID`CW-M1-UI-001`で、非同期成功前は現在値不変、二重入力抑制、遅い応答の扱い、失敗後同要求再送、古いtoken、下書き保持、openとcreateの分離、任意本文の表示記録を重点確認。旧26項目・戦闘・比較の一式再実行を今回の開始条件にしません。
- 実APIがあれば同じオリジンで起動→準備→出発→帰還→変更→再出発／再開を接続し、対応した機能・コードSHA・入力版・保存条件を記録してください。DOM／テスト用応答と実API／実ブラウザー／永続保存、人の評価を分けて報告します。
- 必要成果：接続部品と利用例、M1に必要な起動・保存・再開の導線、公開呼出し対応、条件付きbuild／起動手順、接続済み・未接続の機能一覧と確認記録。統合担当が指定コードを取得して利用できる入口をREADMEへまとめます。
- 実装済みの独立範囲を仕上げた後、実API待ちだけなら不足メソッド・公開項目・再開条件を保存して提出してください。この場合は「接続層提出・実API待ち」であり、U02全体完了とは記録しません。実APIが利用可能なら、その範囲の接続を済ませてから提出します。
- 保存前に自Work・計画・設計の最新を再確認して同期し、通常コミット／非強制Push後、GitHub本文と一致を確認。設計への統合依頼は自Work記録に残し、受領済みとは先取りしません。

最後に成果・実接続範囲・未確認・次の必要入力を報告し、**今回の単位を終了して稼働枠を解放**してください。残り全画面、D03待ちの購入接続、別素材制作へ自動続行しません。

# 設計資料のコミット記録

資料区分：作業資料・Git反映記録。採用仕様の本文は [基本設計](../../確定仕様/カード探索ゲーム_基本設計.md)。

## 現在の反映状態

元リポジトリ `/久乃銑泉/gist/crossweave/` の作業ブランチ `dev_design_tmp_assembly` に、確定仕様と資料整理のコミットを反映した。作業ツリー・インデックス・HEADを一致させ、今回の整理による未コミット差分を解消した。GitHubへのPushは実施していない。

| コミット | 内容 |
|---|---|
| `6be51aa` | 確定仕様を集約し、行動結果の事前表示方針を記録 |
| `e2196ec` | 設計資料を確度別に整理し、検証資料と更新方針を記録 |
| この文書の更新コミット | 資料整理を作業ブランチへ反映し、未コミット差分を解消 |

分岐元は `8509b12b0d0e6fc6e50255aa5ff592450878ab94`。第1コミットは確定仕様1ファイル、第2コミットはREADME・仕様案・作業資料・検証報告と再現資料・原案の配置変更を含む。未採用案や仮条件をGitへ記録したことを、正式採用とは扱わない。

## 前回の不足と修正

当初は `dev_design_docs_organized` にコミットを追加しただけで、現在の `dev_design_tmp_assembly` のHEADとindexは元のまま残していた。このため、整理済みのファイルが未コミット・未追跡の差分となり、ブランチ切り替えを妨げた。

修正に先立ち、元indexに別のステージ済み変更・未解決競合がなく、作業ツリーの全17ファイルが `e2196ec` と一致することを確認した。Gitで元状態を再現し、作業ツリーを保持してHEADとindexを整理後の内容へ揃え、記録の修正を追加コミットした。

`dev_design_docs_organized` は `e2196ec` を指す整理時点のブランチとして残す。現在の作業ブランチには、その後の本記録の修正も含まれる。

## 確認方法

元リポジトリのルートで確認できる。

```sh
git status --short
git log -3 --oneline dev_design_tmp_assembly
```

今後の仕様策定差分は、既存の仕様策定用ブランチまたは新規ブランチへ、日本語メッセージでコミットしてよい。別ブランチへの保存だけで完了とせず、現在の作業ツリーに今回の未コミット差分が残っていないことを確認する。Pushは実施しない。

## 過去の受渡し用bundle

[確定仕様.bundle](確定仕様.bundle) は第1コミットだけを渡すために作成した記録。対象コミットは元リポジトリへ反映済みのため、再取り込みは不要。資料整理全体や、その後の修正は含まない。

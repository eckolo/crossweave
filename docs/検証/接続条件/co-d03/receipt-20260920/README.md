# CO-D03：成果受領と限定再現確認

2026-09-20／WorkID `20260909-design-assembly`／確認ID `CW-M1-D03-RECEIPT-20260920-01`。

**既存D03成果の照合・最新計画同期・専用50件の再現確認を完了。本体のGitHub反映は未完了。** 本書を含む文書記録の保存は、実行コードの公開・UI受領・D03全体完了を意味しない。

## 受領と正規履歴

[CO-D03指示0.2](../../../../作業資料/とりまとめ/着手指示/CO-D03_取得と個体経済.md)と[成果の参照先](../../../../作業資料/とりまとめ/引継ぎ/CO-D03_20260920_成果受領/README.md)に従い、原ZIPを取得した。SHA-256 `e98e858b144924dff0c4f14db40e1761be8731dc5ab99e5851f35868c707748b`、11,031,736 bytes。patchと変更36ファイルのhashは提出manifestに一致する。

設計リモートHEAD `e268d495ef40ccfc4a955465c0d45e97c52fd755` を正規cloneした。未保存差分なし。一時indexへpatchを適用したtreeは `998b8b578445014ae5bfa7c9ab43372f2ea096c0` で、source全753ファイルのpath/blobと一致。ZIPの独立履歴commitを親にしたり、source全体で現在の枝を置き換えたりしていない。

計画 `738cc7b7cfd97a730976e05776d5a4d8171abc39` の全26ファイルを同じパスへ同期した。全体0.8.5／Work別0.8.6／D03指示0.2。変更は再実装開始から成果受領・反映残件へ移る指示であり、ゲーム入力・優先度P0・M1途中到達Bは維持。[全path/blobの同期記録](../../../../作業資料/計画同期/20260909-design-assembly.json)。計画コミット全体のマージは行っていない。

提出patchのWork設定・同期JSONは最新の受領状態へ置き換え、それ以外の34ファイルは原ZIPと一致する。検査に使うコード・入力33ファイルのSHA-256も固定条件と一致。受領したruntimeに追加修正は不要だった。

## 限定再現確認

| 項目 | 今回の結果 |
|---|---|
| 固定したローカルcommit | `6d9353512aeebf728b55fb853354737622661619`。親は正規のe268d495。本体未公開なのでGitHub上の成果SHAとして扱わない |
| 固定tree | `67c40377a99cd5b41320b410c09655032fe71ddb`。受領コードと最新計画・開始記録を含む |
| 環境・コマンド | Node v24.19.0／Linux x86_64／MemoryStore。`node --test test/runtime/d03.test.mjs` |
| 結果 | 50件成功、失敗0、skip 0、終了コード0 |
| 対象 | 帰還候補・購入・変換・所持・準備・次探索・保存再開、一回移行、CAS／保存失敗、公開境界、322札修飾・64抽選条件、実行とpreviewの一致 |
| 固定結果の扱い | 原提出 `CW-M1-D03-001` の結果・保存例・自然進行記録を上書きせず、今回の出力を本フォルダへ分離 |

[結果JSON](results.json)、[実行ログ](node-test.log)、[固定条件・受領照合・hash](verification.json)。検査コードが出力するJSON内のcheck_idは元検査のIDを維持し、この再現のID・日時・固定commitはverification.jsonに記録する。元の3帰還・139行動は提出済みの自然進行記録で、今回同じ3帰還を新たに実行した意味ではない。

原入力0.1、D58内容0.2、rules0.5、save schema1は維持。D03提出版はengine0.6／public0.5。価格・容量・変換率は試行値のまま。D58・旧D02・AU〜BFの全検査は再実行していない。実IndexedDB、描画・入力、実複数タブ、人の評価、リモートD03 CIは未確認。UI/D04へ展開していない。

## 反映制約と残る操作

前回は、D03共通runtimeをGitHubへ反映する `GitHub.create_tree` がOpenAI側の自動安全性レビューで拒否された。提示された理由は **「リクエストの安全性を確認できなかったため」**。この記録からGitHub権限不足・競合・コード不具合とは断定できない。

今回、拒否解除の根拠は確認できていない。拒否対象の本体書込みを再要求せず、git push・ファイル更新API等への経路変更も行わない。安全に独立して進められる文書記録だけを同じGitHubの正規操作で保存する。本体コード・検査コード・workflowの公開許可とは区別する。

本体反映を再開する際の残操作は次のとおり。

1. 拒否対象の本体書込みを許可された手続きで実行できる状態にする。環境変更・通常Pushの一般許可だけを解除根拠にしない。
2. 設計枝と計画の最新HEADを再確認し、今回保全した正規履歴由来の本体差分を照合する。新しい計画があれば全範囲同期し、必要な差分だけ確認する。
3. D03本体・専用検査・元報告を通常保存する。強制Push・独立履歴への置換・不完全な中間treeのref反映を行わない。
4. GitHubの最終ref、commitの親、tree、対象本文を読み戻す。D03専用CIの実結果は実行されてから確認し、保存済み状態・公開SHA・UI受渡しの所在を報告する。

ゲーム仕様の新たな判断待ちは0件。UI向けの個体詳細・価格・変換見積り・準備比較・成功／拒否応答は原ZIPの `source/docs/検証/接続条件/co-d03/ui-handoff.md` と `public-examples.json` に揃っている。公開後に同じパスで受け渡す。UI担当が受領したとは記録しない。

## 文書記録の保存と読戻し

本体・検査コード・workflowを除く文書13ファイルは、同じ `GitHub.create_tree`／通常commit／forceなしref更新により `435f1d6fc065f58b383b0daabdb089306e03af57` へ保存できた。tree `8ac082feb166994c357346e48b14f51b4957cd2a`、親e268d495、refの一致と全13本文・全26計画blobの読戻しを確認した。[読戻し記録](github-readback.json)。この成功を拒否対象の本体書込みの承認とは扱わない。

本体の残差分は原成果と一致する34ファイル（Work設定・同期記録は今回更新済み）。追加のruntime修正はなく、専用検査の固定版からコード差分もない。正規履歴のローカル枝へ公開文書コミットを通常統合し、元ZIPを保持したうえで未反映差分と履歴を再開用に保全する。

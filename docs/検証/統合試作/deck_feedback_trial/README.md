# 山札・環境支援・弱札の比較試作（追加X/Y）

2026-09-09。現在の操作試作は追加Y・基本設計0.38対応。大岩の遮蔽を全員へ及ぼし、弱札の命中を50へ仮調整し、履歴を新しい順にした。性能値と場の表示対象の選び方は試行条件。最適構築・製品バランス・通常の勝率・面白さを実証したものではない。Xの入力・比較結果・HP36局面は過去の再現用に保持する。

## 保存先と実行

このディレクトリのコード・入力・小規模結果の正本はGitHubの `dev_design_tmp_assembly`。大型ZIPは[外部成果物一覧](../../外部成果物.md)のDropboxに保持し、今回変更しない。旧Dropbox文書を更新しない。

Node.jsの標準機能だけで、リポジトリルートから実行できる。

```sh
node docs/検証/統合試作/deck_feedback_trial/ecology_study.cjs
node docs/検証/統合試作/deck_feedback_trial/build.cjs
```

前者はYの `ecology_results.json` と `ecology_cases.json` を再生成する。後者は会話内の試遊用フラグメントを `/workspace/crossweave-shared-hunt.html` に組み立てる。出力先は第1引数で指定可能。ブラウザーの実描画・端末別操作は今回未検査。

Xの再計算は `node docs/検証/統合試作/deck_feedback_trial/study.cjs`。こちらだけが `results.json` と `recovery_checkpoint.json` を再生成する。現在のY画面に旧HP36状態を読み込まない。

## 現在の追加Y

`ecology.js` は元のエンジンを継承し、全員対象のパッシブと弱札の命中設定を追加する。`feedback.js` の `roles` の上に設定し、通常の会心・防御の役割・初期構築はXから変えない。

| profile | 大岩の回避20の対象 | 弱札命中 |
|---|---|---:|
| local20 | V0 | 20 |
| shared20 | 全生存主体 | 20 |
| local50 | V0 | 50 |
| shared50（画面） | 全生存主体 | 50 |

全員には岩自身・P・敵・環境と、後から参加する主体を含む。岩が退場すると全員の支援が消える。岩の軽減8は岩だけ。弱札は威力2を保ち、初期・後続・生成の基本弱札に同じ命中値を設定する。

Xと同じ入口seed32、同じ公開方策の2対象優先×3防御優先×4条件。24経過は重複を含む従属比較。全員／50ではPの命中加算0攻撃が0/127、NPCは9/112であり、双方の硬直解消とはしない。手札・場・回避によって実効威力や命中が0になる条件は残る。NPCの別攻撃の有無は診断専用でPの判断入力や画面へ公開しない。

4,001合法手の今回予測、90回のJSON保存復元、HP収支、local20とX役割条件の全P待機・終端保存状態一致を確認。支援の全員適用・後続・退場解除、岩だけの軽減、防御回避の保持、生成札にも共通の性能を確認する。詳細は[統合検討75〜77章](../../../作業資料/カード探索ゲーム_統合検討.md)と[試験条件追加Y](../カード探索ゲーム_試験条件.md#shared-environment-y)。

履歴は新しい順を既定にし、古い順へ切替可能。データを反転せず表示だけを変える。大岩の全員支援、各主体の回避合計、今回の退場で支援が終わること、命中加算0の内訳を示す。自分の山札一覧を維持し、元の入口HP60から最後まで継続する。回復は使えばHPとして効果が残るため、消滅だけを理由に温存判断が生まれる前提を改め、他の行動を見送る負担と探索全体の総量へ評価を移す。

## 元資料と維持する規則

移行済み外部成果物の回復構築ZIP（Dropbox rev `65b0483757bf512d78594`、11,792,222 bytes）内 `card_stats_trial/` から、必要な編集対象だけを通常ファイルとして取り出した。

| 現在のファイル | 取り込み元 | 元のSHA-256 |
|---|---|---|
| engine.js | playable.js | 63615a41c1b57e8be3caa5a52809671cb3b81c06367a036bc265f967d9ec0f8b |
| input.json | playable_data.json | b8373c7c19c3b1a50fd9cdfd28fb0a5dfe4d368dbe1c9afb0a25b07bf2561981 |
| play.fragment.html | long-play.fragment.htmlから今回更新 | 43f05aaa3354184e65dfd0466662a5d3d6ab4a89b5361fb3805e03371bc8ff01 |

`engine.js` と `input.json` は取得時のバイト列を保持。前者はWでPythonと1,371ステップを照合した演算・循環エンジン。後者には旧試験の説明用メタデータも残っているが、実行条件は本書、Xの `feedback.js`・`study.cjs`、Yの `ecology.js`・`ecology_study.cjs` と各結果ファイルに従う。使用されない主体固定会心の旧メタデータは `prepare` 時に除去する。

会心の参照元は使用札。増加量は借り札になっても変わらず、後続の敵・環境・生成札も同じ登録値を使う。攻防共用、未命中時の会心保持、防御時の場威力→防御・場命中→回避、次の一致で防御終了、不一致なら保持、回収と退場由来消滅、帰還・報酬精算はWのまま。全ドロー順や敵の私有札を画面で公開しない。

P最大HP60、12枚、手札3、回復草1枚・回復16・期限16・回収時消滅、探索内補給なし。最初の環境は弱攻撃12枚であり、防御札を初期供給しない。後続の敵は防御札を持ち込む。共有循環後の所持者と、最初の出現元は別。

## 追加Xで比較した仮設定（履歴）

| プロファイル | 通常札の会心増加 | 回復／弱札／補填 | 防御 | 受け流し | 見切り |
|---|---:|---:|---|---|---|
| original | 50 | 25 | 防御3・回避0・会心50 | 防御2・回避0・会心50 | 防御2・回避0・会心50 |
| slow | 25 | 10 | 防御3・回避0・会心25 | 防御2・回避0・会心25 | 防御2・回避0・会心25 |
| roles | 25 | 10 | 防御3・回避0・会心20 | 防御2・回避0・会心40 | 防御1・回避30・会心20 |
| roles_mix | rolesと同じ | rolesと同じ | 持込1枚へ | 持込1枚へ | 持込1枚を維持 |

`roles_mix` だけ、Pの防御D1枚を打ち払いDへ、受け流しA1枚を速撃Aへ交換する。既存札の構築対照であり、攻防共用札を新設しない。12枚・属性A4/B4/C2/D2・同名2枚上限・元のシャッフル位置は維持する。攻撃6/防御5/回復1から攻撃8/防御3/回復1になる。敵の初期配分は変えない。X作成時の操作画面は `roles`。現在はその上にYの `shared50` を適用する。

## 追加Xの比較範囲と結果の読み方

Wと同じ入口seed32を4プロファイル×2対象優先×3防御優先で24経過比較。対象優先は環境／岩。防御を選ぶ際は防御量／回避／会心加算の各優先で比較し、回避や会心の個性を防御量だけの順位で評価しない。攻撃は今回のHP減少、命中蓄積、札威力、期限の順。回復は欠損16以上、または期限1で正回復できるとき。他の手があれば回復を残す。判断に相手の非公開札や未来乱数を使わない。

同じ入口・同じ後続方策の条件比較。重複する経過を含み、24人・24独立標本ではない。全結果と指標の分母は `results.json`。攻撃札のない手札、防御だけの手札、その連続回数、該当防御札の出現元、会心を伴う命中攻撃、実回復、被害、手数を別計数。HP収支も照合する。「攻撃札なし」は回復札等も含むため「防御札だけ」と同義ではない。

表示残数と実際の山札・手札の一致、全4,429合法手の今回予測、161回のJSON保存復元を確認。山札順の入替と相手の非公開属性の改変で自分の一覧が変わらないこと、後続敵にも札の会心値が適用されること、3防御構築の枚数・属性・同名上限を検査する。机上比較の結果は[統合検討73章](../../../作業資料/カード探索ゲーム_統合検討.md)へ記録。

## 山札と属性枠

自分の未ドロー札を種類・属性・性能ごとに集計し、残数、主効果、会心増加、場の攻防／命中回避補正、期限、設置／一致コスト、消滅条件を表示。手札枚数と初期持込枚数は別欄。初期札は残0枚でも残し、共有循環から受け取った種類を追加する。消滅予定の山札内枚数も表示。メニューは最初から開き、開閉状態を操作のたびに戻さない。

場は属性ごとの枠を維持。自分の現在の山札・手札と公開された場で確認した属性を保持し、未知の属性が場へ出たら枠を追加する。世界全体の属性一覧や、相手の非公開札から枠を作らない。これは今回の表示候補であり、今後出現し得る属性の完全な予告ではない。空枠の保持・イベント切替時の整理は今後のUI検討として残す。

## 追加Xの被弾後局面（過去の再現用）

入口を長くするだけでは被弾しない試遊もあり、前回の人評価は消滅心理を評価できない条件だった。補助試遊は、HPを操作せず、同じ入口から合法な行動で到達した状態を使う。

選定条件を事前に固定：`roles`、山札再構築1回以上、PのHP20〜44、回復16が現在成立、後続敵が生存、後続環境HP30以上、回復以外の手札2枚以上、回復期限3以上。プレイヤーの選択乱数seed0〜127を昇順、各最大40手で調べ、最初の該当を選ぶ。終端結果で選別しない。前置きは回復札以外の合法手から抽選するため、通常プレイの代表例・推奨方策とは扱わない。

最初の該当は選択seed6、P16手目、HP36/60、再構築1回、回復草残6手。回復16、見切り防御1・回避30、踏み込み設置が選べる。`recovery_checkpoint.json` に15手の前置き、全状態、乱数、公開記憶を記録し、入口からの再実行で一致を確認。X作成時の操作画面の「被弾後の判断局面から」は、この前置きを実行していた。Yでこの入口を外し、保存ファイルは過去の再現用として保持する。任意のHP差替えはしない。通常入口からの継続も残す。

その後、回復選択がほぼ定型的との人の所感を受領した。現在防御なし・即ダメージ攻撃なしという選定の不足を統合検討77章へ記録し、この局面への追加回答を現在の作業の前提にしない。回復の必要性だけで心理圧を確認済みにせず、回復後のHP保持と攻撃機会・防御維持を含めて評価する。

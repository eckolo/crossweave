# UI-R-001 検証記録

2026-09-10（日本時間）。操作可能な改善案1の準備・表示内容の照合。人の試遊結果ではない。

## 固定条件と受領資料

- 比較元はPT-Z-001、コード・入力 `e9b5b0a27b6fc51694c347c7e941a28ff97856f5`、基本設計0.39対応、通常入口W seed32。
- 運用・設計の同期は基本設計0.41を含む設計集約、確認・取込済み `968e9e68c7f323558d8fe7255930207ec2903790`。新しい情報開示や知識保持を比較版へ適用しない。
- 試遊側の読取HEADは `1ac403617f72d71bda5f1633aa63da7cd9c9c925`。UI引継ぎ全文、F01〜F03、UI検討メモ、使用版、再現手順、試遊担当設定を確認。原記録の変更・試遊ブランチの丸ごと取込みなし。
- 固定7ファイルの比較元出力は225,311 bytes、SHA-256 `8eb15d38e90d5a69e5149240a756fb2221892e4d19de3d9488e1a9bc14bcdafa`。期待値と一致。

## 既存画面の確認と制限

既存画面の生成後、ブラウザー用ラッパーを作成してローカルプレビューを開こうとしたが、URL制限で拒否された。共有ファイルの文書化された参照方法もURLポリシーで拒否されたため、以後ブラウザーの代替経路を試さず、非ブラウザーのDOM実行へ切り替えた。実描画・実スクロール・端末入力の確認は未完了。

DOMでは既存画面の初期手札（踏み込み／受け流し／牽制）、対象切替、札選択、実行を確認。初期で山札一覧が開き、手札と全種類の効果が同時に並ぶ構造を確認した。最初の操作は対象を大岩へ、受け流しを選択して設置し、2手目の予測・残数・直前経過へ更新されることを確認。これはAIによるUI操作確認でありF01の選択列の復元ではない。

## DOMで確認したこと

実行は `verify.cjs`。結果は[verification.json](verification.json)、使用した正確な選択列は[fixtures.json](fixtures.json)に保存。

- 既存3構成の計74手、441通りの札・対象選択を、元版と改善版へ同じ順に入力。状態・乱数・観測記録が一致した。
- 札・対象を選んだだけでは演算状態を進めない。今回の予測は元版の数値・全文と照合した。例外は退場由来・補填札の期限切れを「回収時に消滅」と明確化した表示1点で、公開情報と実際の回収規則に従う。
- 持込・補給・報酬、相手の傾向・確認札、全履歴・直前経過の内容は元版と一致。非公開の手札・未来の札順・次行動はUIへ追加していない。
- 混入、後続の環境と敵、期限切れ、防御終了、消滅の5局面を固定入力から合法手順で生成し、警告・対象数・山札参照を照合。初期HPや札順の注入なし。
- 詳細を開いた状態は札選択後も維持。履歴の新旧順切替、撤退後の操作無効化、構成選択後の入口からの再開始を確認。
- JavaScriptのDOM実行エラーなし。基準版のengine/feedback/ecology/terrain/inputはハッシュで固定し、変更していない。

DOM検証の再実行にはjsdom 30.0.1を使用（必要Node.js版はjsdomの要件に従う）。UIの組立・プレイ自体には不要。

```sh
cd docs/検証/UI/readability
npm install
npm test
```

生成状態だけを再作成する `node make-fixtures.cjs` はNode.js標準機能のみで動作する。再生成は人の試遊やバランス比較の代替ではない。

## 情報量の参考値

初期状態の閉じたdetails本文を除外し、空白を正規化して数えたDOM文字数は、元版1,418、改善版693。画面の行数・面積・読む速度や認知負担の測定値ではない。情報を削除した効果としてではなく、初期展開する範囲を変えた参考値として扱う。

## 未確認・役割の境界

実ブラウザーでの描画、320／736／1024pxでの折返し・はみ出し、ライト／ダーク時のコントラスト、キーボードとタッチの操作、ユーザーの実端末での読み取り時間・理解・面白さは未確認。CSSには小幅時の縦積みとテーマ対応を実装したが、それを実測の完了とは扱わない。

この制限のため「必要な描画確認まで完了」という段階には達していない。開始手順・操作可能な出力・正確な再現選択列を引き渡し、試遊担当が実画面を取得して確認できるようにする。正式仕様の採用判断、既存F01〜F03の再質問、広いゲームの機械比較は行わない。

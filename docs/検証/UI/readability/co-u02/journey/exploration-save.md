# 0.11：探索だけの中断・編成の確定時保存・公開表示

2026-09-17。Work `20260910-ui-readability`／`ui/readability-20260910`。

ユーザー補足「中断は探索の途中だけ」「山札編集は完了時のオートセーブ等でよい」を優先し、0.10／0.10.1の拠点編集の中断保存を現行導線から外した。履歴の札名、身構・機転・攪乱すべての数値予測、常時軽減を身構へ含める案も受領した。

## 0.11提示後の確認：左下のプレイヤー予測（2026-09-17）

ユーザーから「左下のプレイヤーのパラメータにも予測増減」「他は身構えと軽減の統一案以外問題無さそう」を受領。提示済み0.11のその他の部分は問題なしという確認として記録する。左下の予測と身構・軽減の統一は残件。実ブラウザーの永続保存や未提供機能の完了確認へは広げない。

左下も相手側と同様、札を選んだときに各現在値の横へ符号付きの予測増減を出す。予測窓を開くことを必須にせず、選択札・対象の変更に追従し、実行後は前の予測を消す。身構・機転・攪乱も対象とし、身構の終了・置換、機転の消費を含む今回の一手の解決後の値から差分を表示する。後続する他主体の行動は含めない。

現行`action-forecast.js`と`exploration.js`を確認した。左下にも相手と同じ差分描画が接続されており、本人の余力回復・新しい身構の付与は公開予測から表示できる。一方、身構の終了・機転・攪乱等の解決後総量が公開されていないため、この要望全体は未実装のまま残る。`crit_added`を最終差分として流用すると一閃消費時に誤り、`guard.evasion`も常時支援込みの総量ではない。身構の統一だけを待てば全表示が揃う状態ではない。

設計への追加公開依頼は下表の全能力予測へ集約し、本人も対象に必ず含める。統一案の決定と、既存能力の本人向け予測値公開は別の残件として管理する。今回は要件・受領記録のみを更新し、UIコード・演算・操作画面を変更していない。既存検査の再実行も行わない。

## 反映したこと

- 「探索を中断」は探索phaseだけに表示する。探索途中の本文画面でも使用でき、撤退・帰還せず「続きから」で同じ探索へ戻る。再開成功通知を残さない0.10.1の修正も維持する。
- 札組・心得は「確定」または「確定して出発」の確定処理が成功した時点で本体が自動保存する。編集中の内容は同じ画面内で保持し、未確定のまま新しいUIを開いた場合は最後の保存へ戻る。追加の手動保存は設けない。
- 書出しは本体の保存済みデータをそのまま出す。書出し時に未確定案を`save_draft`する処理を外した。旧版が既に保存した変更案は削除せず、読込み・再比較の互換動作を維持する。
- 履歴と短い行動表示に使用札の名前を添える。同一セッションで公開hand／fieldから取得できた、同一カードIDの名前だけを記憶する。探索を中断して戻っても保持し、別探索・別セッションへ持ち越さない。未取得の過去名は「札（名称未記録）」とする。
- 予測の詳細に、公開済み`crit_added`を「機転の加算（消費前）」として表示する。身構札の`guard.evasion`は「攪乱（この身構）」と明示する。これらを行動後の総量や差分と偽らない。
- 現行試作にある身構と軽減の説明をツールチップ・遊び方に追加した。常時分を身構へ統一する変更は下記の計算側対応と一緒に行う。

## 設計Workへの対応依頼

宛先は既存の設計集約Work `20260909-design-assembly`、統合先は`dev_design_tmp_assembly`。ここに保存した依頼は設計の受領・採用・統合を意味しない。D02の既存APIは接続済みであり、以下の追加項目だけが不足している。

| 項目 | 現状と必要な対応 |
|---|---|
| 常時軽減と身構の統一 | ユーザーは「大岩などの常時軽減も、常時身構が加算される扱いでよいのでは」と提案。別能力として覚える必要を減らし、身構の合計と必要時の内訳（札／常時効果）で表す方向を検討する。UIで名称だけを統一せず、下記の演算差を本体と合わせる |
| 履歴の札名 | `exploration.public_history`に、解決時の使用札の公開名を同一`card_id`へ紐付けて保持・公開してほしい。修飾等がある場合もその時点の公開名を保存する。後から現在の名称や別札の知識で代用しない。保存を開き直しても、手札・場を離れた過去の札を判別できることが必要 |
| 全能力の行動後予測 | `previewAction`に、**本人を含む**公開主体IDごとの解決直後の値を追加してほしい。左下の本人欄にも現在値との差分を表示する。身構（常時分・札分・持続回数の扱いを含む）、機転、攪乱を対象とし、身構終了・機転消費・支援源退場による他主体への変化を反映する。未来のNPC行動は含めず、公開情報から確定する今回一手だけを対象とする |

予測契約では未対応・不明と、変化なしの0を区別できる必要がある。現在の`crit_added`は加算量であり、一閃消費後の残量ではない。`guard.evasion`も札による分であり、常時支援込みの攪乱総量ではない。UIが同じゲーム規則を別実装して埋めない。

常時軽減の調査根拠：現行`src/runtime/core.mjs`は、札の身構に防御側一閃の倍率を掛けてから常時軽減を別に引く。札の身構だけに使用回数・一致時解除と防御側機転の消費が紐付いている。一方、基本設計4.7は常時作用を特性として明示する方針を採用しているものの、仕様案4.6は持続軽減の演算を未確定としている。「硬さを周囲への回避支援と区別する」記述だけでは、身構とは別の計算値にする必要性までは確定しない。

UI側の提案：数値の種類は身構へまとめ、常時／札由来の違いは発生源と持続条件の内訳で表す。本体側では常時分にも一閃を適用するか、常時分だけで防ぐ時にも機転を消費するか、札分が終了しても常時分を維持することを整理する。これらの細部を今回のユーザー発言だけで決定済みにしない。計算・公開値・予測を同じ変更で揃えてからUIを一本化する。

## 今回の確認と保存範囲

[変更20項目](exploration-save-checks.json)は実Campaign＋元MemoryStore＋JSDOMで、生成済み配布版を操作した結果。探索外に中断がないこと、未確定編集と書出し、確定失敗時の保持と再試行、新UIでの確定済み編成再開、本文・探索の中断再開、実設置の札名、予測の加算表記を確認した。保存再開時には本体が札の並びを正規化するため、同じ札と枚数で照合した。旧49件の実行部は再実行していない。

実描画・実タッチ・実IndexedDBは未確認。[実ブラウザー手順](browser-review.md)を今回の保存方式へ更新した。会話内`/workspace/crossweave-exploration-save.html`は実Campaign＋一時MemoryStore、401936 bytes、SHA-256 `b34e5c7553f0d193c82322c89ea029a748f8a5c636d1ff84060621b5099f5b57`。閉じた後の永続保存の証拠にはしない。

変更はUI専用ディレクトリと自Work・受領記録だけ。共通runtime・仕様正本・他Work・固定原本・旧検査証跡は変更しない。保存と読戻し後に修正版を提示して今回単位を区切る。全予測・全履歴名の対応、身構への統一、実保存確認、設計の受領・統合、CO-U02全体の受入は残る。


<a id="gesture-lifecycle-fix"></a>
## 0.11.1：正式版の長押し取消・指付近の札配置を移植

ユーザーの「現段階で進めるべきタスク進めて」を受領し、設計APIなしで進められる正式UI-R-002 v0.15の残る移植を点検した。実接続版に欠けていた以下を反映した。

- 入力捕捉喪失、複数指、ウィンドウの非アクティブ化・タブ非表示、ホイール、手札のスクロール、操作設定変更、参照窓、画面外での解放で長押し待ち・ドラッグを取り消す。相手の長押しも対象にする。
- つかんだ札の選択は取り消し後も保ち、合成クリックだけを抑える。次の実押下とキーボード操作を待機時間で妨げない。捕捉解除より先に入力所有状態を消し、取消の再入を防ぐ。
- ドロップの予測応答を待つ間に入力が中断された場合も、その応答から遅れて自動出札しない。同じ画面の場だけをドロップ先として扱う。
- 札の表示サイズを測り、指が下端から6px内側に触れる位置へ置く。長押しが成立した直後に配置し、上下左右で画面内に収める。固定70px上方オフセットを撤去した。
- 保存検査ページの説明も、編成確定時保存と「探索を中断」の現行方式へ整合した。

[今回24項目](gesture-lifecycle-checks.json)は実Campaign＋元MemoryStore＋JSDOMで、生成済み配布版を操作。入力捕捉・矩形・タッチ相当イベントは明示したテスト入力であり、物理端末の操作確認ではない。取消と遅い予測の競合、正常な設置を一度だけ送ることも確認した。過去の検査一式は再実行・再計上しない。

画面構成・16:9・主体→場→手札の順・中断と保存方式は維持。実描画・実タッチ・IndexedDBは引き続き未確認で、ブラウザー拒否を迂回していない。左下の全能力予測・身構統一・D03の完了ではない。

会話内は`/workspace/crossweave-gesture-review.html`、404160 bytes、SHA-256 `828a4434168e62aa5a82056bd273f7648e3f4f279b879598a57361e7d096562e`。実Campaign＋一時MemoryStore。UI枝から設計枝へのドラフトPRを統合候補として提出し、マージと受領は設計Workに残す。

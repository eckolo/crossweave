# UI0.14.4の追加確認（2026-09-26／実環境では未実施）

1. 敵・地形を一度クリックし、対象印と詳細が同時に変わること。選択中の札を保って予測・操作名・関係線が更新されること。対象切替だけで行動は実行されないこと。
2. 別の相手・札へ続けてクリックし、詳細を手で閉じる手間が不要かを見る。同じ対象の再クリックでは詳細だけが開閉し対象は保持されること。
3. 全体／原寸・明暗で、固定416×384の相手詳細が対象枠・場・手札・行動順・操作ボタンを隠さないか、本文が読めるかを確認。サンプルの通常枚数以外や横スクロール中も記録する。札の詳細と同じ縦横比13:12を保つ。
4. 敵を長く押しても円や別操作が始まらず、離すと通常のクリックになること。移動・スクロール・取消後に対象を変えないこと。札の120ms後の表示・既存のドラッグは維持されること。
5. 実マウス・実タッチ・実キーボードの結果を分ける。DOMと合成イベントの検査成功を実入力の合格にしない。実IndexedDB・複数タブは従来の未確認区分を維持。

旧0.14.2〜0.14.3の「敵ホールドで詳細」や未採用の「敵ホールドで対象指定」は本版では適用しない。今回の対象は新しいクリック操作。正規入口のアクセス拒否を迂回しない。

---

以下は過去の記録。現在の状態は冒頭を参照。

# UI0.14.3の追加確認（2026-09-25／未実施）

1. 全体／原寸・明暗の外観で、拡大した潜水服の上端が相手の横スクロール列で切れず、行動順とも重ならないこと。下側はステータス欄に少し隠れ、名前・数値を妨げないこと。縦横に不要な送りが増えていないこと。
2. 絵の枠外部分に当たっても隣の対象・行動順への入力を妨げないこと。スクロールバーが表示される環境でも確認する。共通札枠・詳細窓・予測線・出札の位置も見る。
3. 普通のクリックで進行円・操作名が点滅しにくいこと。120ms未満のリリース、120ms以後の表示、標準220msでの札ドラッグ／350msでの相手詳細、短い150ms設定を実入力で区別して確認する。遅いクリックは閾値を超えると表示し得る。
4. 取消・押し直し・画面離脱で遅れた表示が残らないこと。取得編成5.3の両タブにも同じ確認を行う。実マウスとタッチの結果は分けて記録する。
5. 札の実画像は未導入。今後、同じ「上部のはみ出し・下部を本文へ重ねる」方針で接続するときに、手札・場・予測・ドラッグの枠サイズと描画余白を一括確認する。

今回17項目のDOM・注入矩形・制御時計での成功を上記の合格にしない。実IndexedDB・複数タブは従来の未確認区分を維持。過去のアクセス拒否を別経路で迂回しない。

---

以下は過去の記録。現在の状態は冒頭を参照。

# UI0.14.2の追加確認（2026-09-25／未実施）

1. 地形の対象枠・行動順に背景サムネイルがなく地形印が出ること。全面背景と潜水服は維持されること。
2. 手札・場・場の予測・ドラッグ札の絵と本文の境界、名称・効果・属性の区画が同じ位置になること。1行／2行の名称、残り行動数、予測印、消費印、異なる効果値で比較する。原寸／全体と明暗の外観で確認。
3. 設置、一致、選択中と異なる札のホールドで、円の操作名がその札のボタン名と一致すること。画面端でも長い操作名が欠けないこと。
4. クリック／ホールドの統一案は本版へ未実装。敵の従来操作を見て、新案の操作性を検証済みとしない。潜水服の画風適合は未了で、今回の画像維持を再承認と扱わない。

実ブラウザー・実入力・IndexedDBの未確認区分は維持する。正規入口のアクセス拒否の迂回や旧検査全再実施を前提にしない。

---

# UI0.14.1の画像・予測枠・ホールド追加確認（2026-09-25／未実施）

[変更28項目](../review/art-hold-checks.json)と以下の実環境確認を区別する。正規入口のアクセス拒否を別経路で迂回していない。

1. 下記FHD条件で、探索・出発本文・帰還・出発前の背景、潜水服の全体と手番肖像を確認。画像の縦横比、切り取り、名称・数値の可読性、明暗の両外観を記録。背景は原本1536×1024を拡大した仮素材で、正式な高解像度・再訪差分の認定対象にしない。
2. 手札を選択して場に置く予測を出し、配置済み札と予測札の内側の線、絵・名称・属性・数値の位置を比較。「予測／消費」の印で本文が押し下がらず、重なりもないことを全体／原寸で確認。
3. 相手のホールド開始直後に「詳細」と進行リングが出て、350msで詳細窓へ移ること。手札および取得編成の札・心得では「移動」と円が出て、初期220msでドラッグへ移ること。画面端と縮小中も円・操作名が見えること。早いリリース・送り・取消・倍率変更・画面遷移で円が残らないこと。
4. 実マウスと物理タッチを分け、指で隠れないか、開始の気付きやすさ、通常のボタンや余白パンで不要な円が出ないかを確認。新しい表示の成功をIndexedDB・複数タブ確認の代用にしない。

今回の実機確認は未実施。取得画面構造の再承認・通しプレイをユーザーへ求める条件ではない。

---

# UI0.14.0のFHD追加確認（2026-09-25／未実施）

[探索FHD成果](../journey/full-hd.md)の実装・JSDOM26項目と、以下の実機確認を分離する。今回は過去の正規入口の `ERR_BLOCKED_BY_CLIENT` を別ポート・別browser・file URL・新規ホストで迂回していない。認可された確認環境で実施した結果だけを合否へ記録する。

1. 版・オリジン・OS・browser・CSS viewport・devicePixelRatio・OS／browser倍率を固定して記録。まずゲーム描画領域1920×1080を確保する。確認用操作帯とbrowser chromeはゲーム寸法に含めない。
2. `review/index.html?case=explore-d03` から人物・場・手札・下部操作の収まり、札248×208、詳細・予測・共通窓520×480、最大名称の改行・実フォント・数値の可読性を確認。主画面の縦スクロールがなく、長い一覧末尾と主要操作へ到達できること。
3. 本文内の全体／原寸切替、または入口の利用可能幅変更で、状態・選択・窓内容・予測線を保持すること。縮小は提示方法でありスマホ対応の合格にしない。実ポインターで掴み位置・ドロップ先、保持前の横送り、余白パン、保持中の端スクロール、画面外ドロップ、resize・非アクティブ化・capture喪失時の取消を確認。
4. `entry-d03` で出発本文→探索、探索の撤退→帰還→拠点、`carried`／`hub-d03` で前後の表示を確認。探索窓→共通の調査記録、親子窓の並びと本文末尾・操作部も確認。実タッチは物理端末・実効表示領域を別に記録する。
5. 永続保存は通常の `co-u02/index.html` と同一オリジンの実IndexedDBで実施。下記旧手順5の再開・輸出入・失敗と再試行・2タブを確認する。MemoryStoreの成功を代用にしない。取得5.1は未接続なので、旧取得方式での保存成功を新方式の受入と扱わない。

CO-D04／CO-P02への入力として保存。実施者・条件・版・結果・未実施項目を併記し、実施前にUIから合格を先取りしない。ユーザーに同じ取得画面の再承認や通しプレイを求める手順ではない。

---

# CO-U02-B後の実機確認（未実施・旧寸法は上記へ更新）

本手順はCO-D04／CO-P02への入力。UI0.12.0の実装・DOM確認と、実機合格を混同しない。今回の依頼B4は入口・確認記録・操作画面・本手順の提出まで。

既存の正規HTTP入口の`review/index.html`を用いる。以前の正規Browser接続は`ERR_BLOCKED_BY_CLIENT`。別ポート・別browser・file URL・新規ホストで拒否を迂回していない。許可された同一入口で実機確認できる環境から実施する。

1. `case=offers`で修飾詳細→「編成と比較」→購入前／編成後の順序、札組／心得の往復、現在・案・確定後の金額と所持を確認。`case=owned`でロック解除、変換額、最後の1点の警告、実行後の表示を確認する。
2. `case=carried`で撤退リザルト→拠点→取得の持越し、`case=return-d03`で帰還確認前の書込み禁止と資金不足を確認。これらの入口は確認用MemoryStoreで、既存セーブを書き換えない。
3. `case=explore-d03`で本人と全相手の一手後差分、場の予測、身構・攪乱詳細、履歴名、山札内訳、文章の既読／未読、主体から内容版に対応する調査記録を確認。付与札の具体場面は自然進行または設計から追加の自然保存を受け取り指定する。公開例の部品試験を自然進行としない。
4. 1024／736／600／320pxの16:9枠で主画面がスクロールせず、一覧末尾と確定・変換操作へ到達できること。小窓の末尾は小窓内でスクロール可。resizeで案・窓の内容を保持し、親子窓が操作元を隠し続けないこと。長押し、札の横送り、ドラッグ、取消、演出中の入力は実機で確認する。
5. 永続保存は通常入口`co-u02/index.html`へ戻して確認する。編成確定・購入・ロック・変換→再読込みとブラウザー終了後のopen、書出し→空slotへのimport、保存失敗→同request再試行、2タブで古いview→open・再選択を記録する。実IndexedDBに接続した版・オリジン・端末・セーブを固定する。

人に求める評価は、収まりと主要操作、帰還→拠点→比較・確定→再出発の理解、現在／案／確定後の金額・所持・装備の区別の3点。正式画像・演出の前後を明記する。合否は実施後に当該担当が記録し、UIから受入済みと先取りしない。

# CO-U02 停止・未反映記録

この本文は前回Chatの記録を保全したもの。最新の受領・保存状況は末尾「Workモード再開分の保存確認」と[実接続結果](実接続結果.md)を参照。

2026-09-15 JST。同じWorkID 20260910-ui-readability。本単位で区切る。CO-U02全割当完了・ユーザー確認済みとはしない。

## 保存できたもの

計画0.6の12ファイルを同一blobで同期。192fbc9で設計eb28efbまでを取込み、続いて先行実API68da4ffの29変更パスを照合して取込。UI専用の旧資料・コードと全履歴を保全した。旧Work設定618行は同じWorkディレクトリのbefore-CO-U02へ元blobのまま保存し、従来パスには最新停止地点を記載した。

UI-G-001 v0.1.1のcode_commit空欄を公開HEAD 3db9163で補完。旧28件の検査・当時の実表示未了の履歴は書き換えず、今回の追加確認を別欄で記録する。

## 新コードの保存制限

application.jsを書き込むGitHubツール要求が「リクエストの安全性を確認できない」としてOpenAIによりブロックされた。詳しい理由は不明。別API・分割・形式変換による同内容の再送を行っていない。一部の新コードは中間tree objectに存在するが、枝に公開した完了成果とは扱わない。この保存ではそれらを半端な実行入口として反映せず、既存UIと本体同期・記録のみを残す。

**新UIソース、生成物、検査スクリプト、入力投影、検査報告、画像、SHA-256目録は会話添付crossweave-co-u02-handoff.zipへ保全。新UI実装はリポジトリ未反映。** 同名ZIPを見つけただけでマウント済みとは扱わず、実ファイルの存在と目録を確認すること。

## 実装・確認の到達点

非同期controller注入、保存成功後だけ現在値更新、二重入力抑止、同じ要求IDによる再試行、stale時の下書き保全・再選択、帰還取消意図の基礎IDだけの再投影。v0.1.1の描画を再利用し、保存失敗後に比較窓が再試行ボタンを隠す問題を修正した。

Campaign create/open/import/exportの独立入口、本文の実表示後だけのadvance:false記録、現在公開viewによるscene/phaseの切替。D02で確認した辞書形式actors/field、手札remaining、record_displayed_text能力、openによる再読込みへ追従。実モジュール入口はsrc/runtime/campaign.mjs。API未提供待ちではない。

探索は合法choiceと予測・実行を渡す最小描画とrenderer注入口まで。正式UI-R-002 v0.15の完全移植ではない。行動順アイコン・関係線・予測・詳細なhover/dragとの連携、解放名等の公開詳細、S03〜S18の実統合は残る。原本を置換しない。

CW-M1-UI-001：旧描画29件、Node非同期境界25件、新画面部品13件。親幅1024/736/600/320px、最後の札・確定操作・詳細末尾・resize保持を実Chromiumで確認。新部品では失敗後の再試行、輸出入、本文を開く前／viewport外では未記録、本文表示後の記録と合法操作を確認した。

検証環境はNode v22.16.0、headless Chromium 144.0.7559.96。file URLナビゲーションは管理制限で拒否されたため再試行せず、部品はメモリー内DOM入力で確認。実Campaign＋IndexedDB・同一オリジンの保存／再開は未実施。物理端末・ユーザー受入は未検証。旧28件の重複検査はしていない。

## 操作版とユーザー確認

会話のcrossweave-ui-check.htmlは実セーブなしのBC/空状態の表示確認用。新実API接続入口とは別である。ユーザーには、画面内に収まり末尾へ届くか、準備→比較→確定と帰還→次の準備が分かるか、現在値と変更案・確定後が区別できるかを確認する。まだ確認済みにしない。

## 次の受渡し

設計Work20260909-design-assemblyへ、68da4ffを接続先として固定した添付成果の受領・照合を依頼する。新UIコードのリポジトリ反映、正式探索UI移植、実保存・再開・三帰還の結合確認、ユーザー確認を残件とする。今回のUI作業枠は終了。CO-D03や残り全画面へ自動続行しない。

## Workモード再開分の保存確認（2026-09-15）

成果コミット [33c7ac6747452bafd6b35e0f85933c5903bd4fa4](https://github.com/eckolo/crossweave/commit/33c7ac6747452bafd6b35e0f85933c5903bd4fa4) を同じUI枝へ通常コミット・非強制ref更新で保存した。技術同期コミットはb20fccf374aeb652406ecb99084d4eb872e03019。GitHubのブランチrefと全637treeエントリー（blob・subtree）をローカル内容と照合し、application.jsと新規検査2結果の本文もSHA-256まで一致。計画全12blob・正式UIと固定試遊5範囲の不変を再確認。詳細はverification/work-mode/save-readback.json。

保存済み操作画面は模擬応答・実セーブなし。実接続コードは別入口index.html。実Campaign＋設計MemoryStore46件、実Campaign＋JSDOM28件合格。実ブラウザー描画・IndexedDB、正式UI完全移植、D03、ユーザー確認は未了。今回の作業単位を終了しUIの稼働枠を解放する。設計20260909-design-assemblyへ、この成果コミットのco-u02一式・自設定・受領記録の統合を依頼する。UIからの逆方向統合・相手の受領済み扱いは行わない。

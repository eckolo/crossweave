{
  "name": "crossweave-ui-readability-verification",
  "private": true,
  "description": "Optional DOM verification only; the playable build has no dependencies.",
  "scripts": {"test": "node verify.cjs", "fixtures": "node make-fixtures.cjs"},
  "devDependencies": {"jsdom": "30.0.1"}
}

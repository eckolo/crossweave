# VD-001 確認記録

版：0.2／2026-09-12。表現の比較。ゲームの合法手・性能・面白さの試験ではない。

## 制作物

- 1536×1024の情景比較ボードA・B・C・D、B・Dの日本語ラベル版、3道具の旧形態見本。
- 生成は内蔵画像生成。入力プロンプトは `docs/作業資料/ビジュアル・演出/制作元/`。A〜C初稿→B・Cの差別化→B冷たい鋳造所→D柔らかさ→B・D日本語ラベル、という履歴。画像が同じ構図を厳密に保持する保証はなく、画材差だけの統制試験ではない。
- 模式的な行動演出 `motion.fragment.html` と、閲覧用 `preview.html`、再生成 `build.py`。3対象・3結果・4時点を切り替える設計。ゲームエンジン・音源は未接続。

## 確認できたこと

- 生成画像を目視し、画風差、場面の対応、Bの冷えた鋳造所補正、Dの柔らかい明暗、日本語ラベル3件と位置を確認した。
- B・Dの画風についてユーザーの良好な所感を受領。暗さは場面に応じた柔軟な扱いとした。
- 模式演出のJavaScriptは `node --check` で構文エラーなし。閲覧用HTMLは `python build.py` で生成した。

## 未確認・限界

- Playwrightはライブラリのみで、対応Chromium実行ファイルが未設置。ローカルのブラウザー描画確認は起動前に停止し、スクリーンショット・操作成功の記録は取得できなかった。表示幅、実際の切替・アニメーション・縮小動作を確認済みとしない。
- 初回の記号的な模式演出は旧前提の不透明な札形状を含んでいた。再開後に半透明の長方形へ更新したが、新しい画風の最終アニメーションではなく、作用結果の区別を検討する参考コードである。現行UIを置き換えない。
- 完成した背景のレイヤー分離、透明な道具素材、最終の札絵、正式UIとの合成、音、端末操作、演出の反復疲労、制作工数は未検証。
- 道具画像の札材質と机上の札束を含む旧情景は世界観1.5時点の案。Dでは半透明札へ補正。Bは見た目への所感の基準として残し、旧の物理的札束まで採用しない。

## 再開

画像の比較は[ビジュアル入口](../../../仕様案/ビジュアル・演出/README.md)から。模式演出を確認する場合は、`python docs/検証/ビジュアル・演出/VD-001/build.py` を実行し `preview.html` をブラウザーで開く。表示試験の新しい実行環境・画面幅・入力を記録し、今回の未確認を置き換えた範囲だけ更新する。

## 会話上限からの再開時の確認

固定入力は保全コミット `5863178` のVD-001を基に、本記録を含む変更コミットの `motion.fragment.html` へ加えた修正。必須同期元 `3e9b9448093220eabfadde7b23e400a593a665d3` は `1963396cebfe4d688f4278776cb780d6be09379c` で取り込んだ。ゲームの処理試験を新しい条件へ差し替えていない。

- ブラウザースキルからCloud Browserへ接続できたが、ローカルHTMLのfile URLはURLポリシーによって拒否された。別経路で迂回せず停止。スクリーンショット、実描画、実操作の成功記録はない。
- [check.cjs](check.cjs) はブラウザーを使わず、DOM・Canvasの代用と仮時計でイベント処理を確認する。再生中の説明追随2件、通常終了、探査のみ、対象変更時の旧予約解除、一閃の説明、動き軽減、非表示での停止の8件を確認。[結果と入力SHA-256](static-checks.json)を保存。幅320という数値を与えるが、実機320pxでの視認性を検証したことにはならない。
- `build.py` で閲覧用HTMLを再生成。ソースと生成物の一致を点検した。幅が狭い場合は640基準の図形を比例縮小する修正を含むが、縮小後の実際の読み取りは未確認。
- 新しい背景案は別題材ART-BG02で、画像を目視。日本語ラベル、人物・潜水服を背景へ描き込んでいないこと、塩の跡と奥行き、柔らかな光を確認。1672×941px、ほぼ16:9。逆流の因果の理解・正式UIとの合成・ユーザー所感は未確認。
